{
  "name": "jekyll-examples-circleci-docs",
  "version": "0.0.1",
  "description": "jekyll-blogsearch usage example with circleci-docs",
  "private": true,
  "scripts": {
    "build": "./download.sh && ./build-database.sh",
    "clean": "rm -rf circleci-docs *.db.wasm",
    "update-patches": "./update-patches"
  }
}
