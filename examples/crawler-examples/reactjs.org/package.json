{
  "name": "crawler-examples-reactjs.org",
  "version": "0.0.1",
  "description": "blogsearch-crawler usage example with reactjs.org",
  "private": true,
  "scripts": {
    "build": "yarn build:pages && yarn build:database",
    "build:pages": "./build-page.sh",
    "build:database": "blogsearch-crawler",
    "clean": "rm -rf reactjs.org.blogsearch.db.wasm reactjs.org page_source.tar.gz node_modules",
    "upgrade-deps": "./upgrade-yarn.sh"
  }
}
